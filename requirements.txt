# SnapPDF Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
Pillow>=9.0.0
reportlab>=3.6.0
pandas>=1.3.0
PyPDF2>=3.0.0

# Optional dependencies
# tkinterdnd2>=0.3.0  # For drag-and-drop support (optional)

# Note: tkinter is included with Python standard library
# If tkinter is not available, install it via your system package manager:
# - Ubuntu/Debian: sudo apt-get install python3-tk
# - macOS: included with Python from python.org
# - Windows: included with Python installer
